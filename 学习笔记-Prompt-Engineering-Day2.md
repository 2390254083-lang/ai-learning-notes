# Day 2：Few-shot + Chain-of-Thought + 结构化输出

> 第 1 周 · Prompt Engineering
> 用时：1-2 小时

---

## 一、Few-shot：给 AI 看例子，比说十遍规矩管用

**原理：** 大模型本质是"模式匹配器"，你给它看的示例格式，它会自动模仿。给它看一个翻译示例，比描述十句"要流畅、要准确"管用。

| 策略 | 示例数量 | 适用场景 |
|------|----------|---------|
| Zero-shot | 0 个 | 简单任务 |
| One-shot | 1 个 | 固定输出格式 |
| Few-shot | 2-5 个 | 格式复杂或需要特定风格 |

---

## 二、Chain-of-Thought（思维链）：让 AI "先想再说"

**原理：** 直接让 AI 给答案，它可能瞎猜。让它"先分析，再给答案"，准确率大幅提升。

**做法：** 不写"好好想想"，而是写出具体的思考步骤。

```
❌ 直接问：总结这段文字
✅ CoT：
  第一步：列出这段文字中最重要的 3 个事实
  第二步：判断核心观点是什么
  第三步：用一句话概括核心观点
  第四步：用 2-3 句话补充关键事实
```

---

## 三、结构化输出：让 AI 返回 JSON

**原理：** 在 prompt 里给出 JSON schema + 一个示例，AI 就能稳定输出 JSON。

关键三点：格式定义 + 示例 + 明确字段说明。

---

## 四、实机测试结果

测试文章：景德镇手工制瓷业遗址入选世界遗产名录

### 原版 prompt 输出

内容详细但偏啰嗦，用了加粗和多级标题，AI 不确定要什么格式所以"多给一点"。

### CoT 版（分步骤思考）输出

内容更简短，只保留了最核心的 3 个要点。AI 被思考流程约束，没机会发散。

### JSON 版（结构化输出 + 示例）输出

逻辑清晰，五大组成部分各一行子项目，一眼看到重点。格式模板 + 示例双重锁定。

**结论：** 总结类任务——JSON 结构化输出效果最好；翻译类任务——CoT 反而可能过度加工，One-shot 更合适。

---

## 五、今天的关键一句话

> Few-shot 给格式，CoT 给思考路径，结构化输出给约束。三个技巧各管一块，组合使用效果更好。
